class Solution:
    def isPalindrome(self, s: str) -> bool:
        s = ''.join([c for c in s.lower() if c.isalnum()])
        return s==s[::-1]
        
        
# function isalnum(): to check if it is either alpha or number
